import React from 'react';

/* const useStyles = makeStyles((theme) => ({
    '@global': {
        '#SnackbarParent': {
            [theme.breakpoints.up('md')]: {
                ' & > div': {
                    maxWidth: `${theme.breakpoints.values.sm}px`
                }
            }
        },
        'input[type="time"]::-webkit-clear-button': {
            '-webkit-appearance': 'none',
            display: 'none'
        },
        'input::placeholder': {
            textTransform: 'none'
        }
    },
    content: {
        margin: `${spacing80} auto 0 auto`,
        maxWidth: '1380px'
    },
    root: {}
}));
*/

const App = () => {
    return (
        <React.Fragment>
            <div>hi</div>
        </React.Fragment>
    );
};

export default App;
